def export_video(scene):
    # Simulate export logic (replace with ffmpeg or moviepy)
    print(f"[Exporter] Rendering video for scene: {scene['description']}")
    return f"/static/videos/{scene['style']}_{scene['id']}.mp4"
