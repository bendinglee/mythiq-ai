def generate_quest(title="Reclaim the Archive"):
    return {
        "title": title,
        "objectives": [
            "Find the memory shard",
            "Decode its embedded signal",
            "Return it to the reflector"
        ],
        "reward": "Ability to rewrite history"
    }
